/**
 * Writer-export input adapters for the current MVP reader boundary.
 *
 * <p>This package owns writer {@code imageSequence} folder resolution, {@code frame-sequence.txt} parsing as an MVP
 * writer-export validation helper, lossless PNG frame loading, and writer-compatible pixel hash validation. These
 * concerns stay outside source-neutral frame, decoded-content, and restore packages.</p>
 */
package com.alx4j.jab4j.reader.writer;
